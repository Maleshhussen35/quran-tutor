services:
  - type: web
    name: quran-ai-tutor
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python qura.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
